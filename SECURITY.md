# Security policy

请勿提交 API Key、Token、密码、Cookie、连接串、私有道路规范、用户输入、诊断案例或运行日志。`.env` 和 `data/` 下的运行数据默认被忽略，但发布前仍应检查 Git staged files。

发现安全问题时，请不要在公开 issue 中附带密钥、原始资料或可复现的私有数据。请通过项目维护者在 GitHub 资料页提供的非公开联系方式报告，并说明影响范围、复现步骤和已采取的缓解措施。

部署者负责为工作台、Qdrant、数据库和导出文件配置网络隔离、身份验证、授权及备份策略。默认开发配置只适合本机使用。
